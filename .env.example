GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=
VITE_GAS_API_URL=
VITE_GOOGLE_SPREADSHEET_ID=
VITE_GOOGLE_SHEET_ID=
NEXTAUTH_URL=

# Supabase (server-side only -- service_role key must NEVER be exposed to the frontend)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# One-time refresh token for the Sheets->Supabase migration script (Phase C).
# Obtain via `npm run migrate:auth` -- do not set manually.
GOOGLE_SHEETS_MIGRATION_REFRESH_TOKEN=
